## Начало
Создаём файл `Dockerfile`:
```dockerfile
From 

WORKDIR \app

RUN

CMD [""]
```

Сборка и запуск
```shell
sudo docker build -t my-app

sudo docker run -it --rm `app_id` or run `app_id`
```



